import { City } from './enum';

const OFFER_SCREEN_IMG_COUNT = 6;
const RATING_STARS_COUNT = 5;
const DEFAULT_CITY = City.Paris;

const RATING_TITLES = ['perfect', 'good', 'not bad', 'badly', 'terribly'];

export { OFFER_SCREEN_IMG_COUNT, RATING_STARS_COUNT, RATING_TITLES, DEFAULT_CITY };
